CREATE TABLE Todo (
    id INTEGER NOT NULL PRIMARY KEY AUTOINCREMENT,
    title TEXT NOT NULL,
    description TEXT,
    completedAt TEXT
);

insertTodo:
INSERT INTO Todo(title)
VALUES(?);

removeAllTodos:
DELETE FROM Todo;

selectAllTodos:
SELECT Todo.*
FROM Todo;